Q1.why is list used?
Ans-List is data structure in Python.It is used to store the sequence of element.
List is mutable ie it can be changed.

Q2.How do lists and set's differ?
Ans-List is a type of ordered collection that maintains the elements in insertion order 
while Set is a type of unordered collection so elements are not maintained any order. 
List allows duplicates while Set doesn't allow duplicate elements.
List are faster than set.

Q3.Application of Dictionaries with a real world example.
Ans-Dictionaries are used when you need a lot of organization because the data is very dependent on each other.
suppose needed to know if a group of people voted yet today(Election day)
In order to store unordered key-value-pairs.
